# CLAUDE.md

## 本文件定位

本文件是**硬规范**：排版、代码行宽、缺字符号、文风四条，写每一章都必须遵守。
写作理念与章节设计方法见 `dev/design-rationale.md`；
本书的具体创作约定（读者画像、章节大纲、进度）见 `dev/plan.md`。

## 写作规范

### 呈现形式

- **代码、图、案例是正文的主体**：文字只承担三件事——串联、点睛、复盘。
- 解释一个概念，先给代码或图，再给一句压得住的话；整段文字解释只允许出现在机制转折与复盘归档处。

### 图表题注

- **编号**：图、表须按章**独立连续编号**，格式统一为「图 N-M」与「表 N-M」（N 为章号，M 从 1 起递增）；编号的增删仅限在本章内重排。
- **范围**：仅给正文中显式以「图 N-M / 表 N-M」字样引用的图表分配编号、添加题注；字段字典等纯展示性列表不予编号。
- **位置**：图题置于**图下方**，表题置于**表上方**，题注与图表主体之间必须保留一个空行。
- **格式**：题注格式为「编号 + 一个半角空格 + 题注短语」，句末严禁添加句号。例：`图 1-1 同一个文件，两种读法`。
- **引用**：正文引用必须使用固定全称（如「图 1-1」「表 2-3」），禁止使用「下图 / 上图 / 上表」等随排版漂移的相对方位词。
- **写法**：直接在 Markdown 正文中手动编写题注（站点与 PDF / EPUB 共用同一源文件）；构建流程已禁用 Pandoc 自动题注功能，不会产生重复。

### 正文代码行宽

- **控制每行 ≤ 72 个半角字符**：PDF 渲染所用的 LaTeX 代码块不支持自动折行，约 75 个半角字符即溢出页边距，72 为预留安全余量；超出必须拆行，严禁通过「长单行表达式 + 尾随对齐空格」将行宽撑至 75 字符以上。
- **保障拆行可直接复制运行**：拆行不得破坏代码的可执行性。SQL 语句可直接换行；Python 须将长表达式置于括号内续行，长字符串须利用相邻字面量自动拼接，严禁产生语法截断。
- **特例处理**：缺乏跨 Shell 移植方案的单行 Shell 命令（如 `python -c "..."`）允许超宽，但须尽量压缩空白或改用更简短语法。
- **最长行自查**：写完代码块后目测复核最长一行，拆行宁早勿晚。

### 正文符号缺字

- **严禁使用 `≤` 与 `≥` 字符**：因 PDF 渲染所用的 Latin Modern 正文字体与代码等宽字体均缺少 `≤` 和 `≥` 字符，会导致排版空白且仅在构建告警中显现，所有比较条件必须编写为行内代码格式的 `<=` 与 `>=`（使用 ASCII 字符以保证字体兼容性）。例如：禁止编写「里程 ≥ 10」，须写作「里程 `>= 10`」。
- **严禁使用圈码 `①`–`⑩`**：Latin Modern 粗体同样缺少圈码字符（构建日志报 `Missing character: There is no ① (U+2460)`），序数列举改用「第一 / 第二 / 第三」或「其一 / 其二」等文字形式。
- **兜底检查**：定稿前全文搜索 `≤`、`≥` 与 `①`-`⑩`，命中处须改写为上述替代形式。

### 正文文风

- **避免单字形容词概括抽象内容**：不用单字形容词概括抽象事物（如「这章很薄」），改用自然的说法。
- **摒弃防御式表述**：不写防御性文字；默认不会发生的事不解释。
- **表意精炼**：一个意思只说一遍。
- **避免对称排比堆叠**：禁止机械堆砌「既……又……」「不仅……更……」等排比句式。
- **精简程度副词**：最大程度删减非必要的程度副词（如：非常、比较、基本、通常等）。

### 叙述视角

- **文本中只有作者与读者两方**：叙述以作者对读者直接讲授的口吻进行。
- 禁止出现「项目」「仓库」「写作过程」等概念：书中不存在本书的创作工程，不存在草稿、分工、进度、改版历史；不得引用已删除或未写成的内容。
- 随书交付的材料（示例代码、随附实现）按读者拿到的「书」措辞（如「本章代码」「随书示例」），不按写作侧的「项目」措辞。
